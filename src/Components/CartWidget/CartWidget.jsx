import cart from "./assets/cart.png"

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt="cart-widget" style={{ height: 50}}/>
            0
        </div>
    )
}

export default CartWidget 